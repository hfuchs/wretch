" 2013-05-10, A minimal vimrc in order to run the tests in tests/ with
" only wretch loaded.

" The vimrc /has/ to remain in the project root and run_tests /has/ to
" be called from there as well.  I'm lazy.
set runtimepath+=.
set nocompatible
set formatoptions+=cro

" This is more usually set in debian.vim (see /etc/vim/vimrc).
set backspace=indent,eol,start

syntax on
filetype plugin indent on

"let g:wretch_default_body_type = "mail"

" Profiling, remember to move the file away for comparisons.
"profile start vim.profile
"profile! file *wretch*
